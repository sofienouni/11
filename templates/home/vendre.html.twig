{% extends 'base.html.twig' %}

{% block title %}Vendre Votre Bien!{% endblock %}

{% block body %}
    <section class="w3l-apply-6">
        <!-- /apply-6-->
        <div class="apply-info py-5">
            <div class="container py-lg-3">
                <div class="heading text-center mx-auto">
                    {% if location|default %}
                        {% for texte in textes %}
                            {% if texte.page == 'Déposer Une Annonce'%}
                            <h3 class="head text-white">{{ texte.titre }}</h3>
                            <p class="my-3 head ">{{ texte.texte }}</p>
                            {% endif %}
                        {% endfor %}
                    {% else %}
                        {% for texte in textes %}
                            {% if texte.id == 10 %}
                                <h3 class="head text-white">{{ texte.titre }}</h3>
                                <p class="my-3 head ">{{ texte.texte }}</p>
                            {% endif %}
                        {% endfor %}
                   {% endif %}
                </div>
                <div class="apply-grids-info row pt-5 mt-3">
                    <div class="apply-gd-left col-lg-7">
                        <div class="row">
                            <div class="col-sm-6 sub-apply">
                                <div class="apply-sec-info text-center">

                                    <span class="fa fa-university mb-4"></span>

                                    <div class="appyl-sub-icon-info">
                                        {% for texte in textes %}
                                            {% if texte.id == 11 %}
                                                <h5><a href="#">{{ texte.titre }}</a></h5>
                                                <p>{{ texte.texte }}</p>
                                            {% endif %}
                                        {% endfor %}
                                    </div>

                                </div>

                            </div>
                            <div class="col-sm-6 sub-apply mt-sm-0 mt-5">
                                <div class="apply-sec-info text-center">

                                    <span class="fa fa-bath mb-4"></span>

                                    <div class="appyl-sub-icon-info">
                                        {% for texte in textes %}
                                            {% if texte.id == 12 %}
                                                <h5><a href="#">{{ texte.titre }}</a></h5>
                                                <p>{{ texte.texte }}</p>
                                            {% endif %}
                                        {% endfor %}
                                    </div>

                                </div>

                            </div>
                            <div class="col-sm-6 sub-apply mt-5">
                                <div class="apply-sec-info text-center">

                                    <span class="fa fa-cubes mb-4"></span>

                                    <div class="appyl-sub-icon-info">
                                        {% for texte in textes %}
                                            {% if texte.id == 13 %}
                                                <h5><a href="#">{{ texte.titre }}</a></h5>
                                                <p>{{ texte.texte }}</p>
                                            {% endif %}
                                        {% endfor %}
                                    </div>

                                </div>

                            </div>
                            <div class="col-sm-6 sub-apply mt-5">
                                <div class="apply-sec-info text-center">

                                    <span class="fa fa-hospital-o mb-4"></span>

                                    <div class="appyl-sub-icon-info">
                                        {% for texte in textes %}
                                            {% if texte.id == 14 %}
                                                <h5><a href="#">{{ texte.titre }}</a></h5>
                                                <p>{{ texte.texte }}</p>
                                            {% endif %}
                                        {% endfor %}
                                    </div>

                                </div>
                            </div>
                        </div>
                    </div>
                    <div class="apply-gd-right col-lg-5 mt-lg-0 mt-5">
                        <div class="apply-form p-md-5 p-4 mx-auto bg-white mw-100">
                            {% for texte in textes %}
                                {% if texte.id == 15 %}
                                    <h4>{{ texte.titre }} </h4>
                                    <p class="mt-3">{{ texte.texte }}</p>
                                {% endif %}
                            {% endfor %}
                            <h4>Pouquoi Nous Choisir </h4>
                            <p class="mt-3">Notre expertise marketing permet de diffuser votre bien sur les principaux portails immobiliers, sur nos newsletters ciblés et sur nos vitrines au coeur de nos agences de Tunis.</p>
                            <p class="mt-3"> Notre expertise sur la législation tunisienne vous assistera pour les démarches administratives. Notre équipe est polyglotte et vous aidera au mieux à réussir à vendre votre bien.</p>
                            <hr>
                            {{ form_start(form) }}
                                <div class="form-group">
                                    {{ form_row(form.nom,{ 'attr': {'class': 'form-control'} }) }}
                                </div>
                                <div class="form-group">
                                    {{ form_row(form.prenom,{ 'attr': {'class': 'form-control'} }) }}
                                </div>
                                <div class="form-group">
                                    {{ form_row(form.email,{ 'attr': {'class': 'form-control'} }) }}
                                </div>
                                <div class="form-group">
                                    {{ form_row(form.telephone,{ 'attr': {'class': 'form-control'} }) }}
                                </div>
                                <div class="form-group">
                                    {{ form_row(form.type,{ 'attr': {'class': 'form-control'} }) }}
                                </div>
                            {% if location|default %}
                                {% set optype = 'A Louer' %}
                            {% else %}
                                {% set optype = 'A Vendre' %}
                            {% endif %}

                                <div class="form-group">
                                    {{ form_row(form.operation,{ 'attr': {'class': 'form-control'}, 'value' : optype }) }}
                                </div>
                                <div class="form-group">
                                    {{ form_row(form.ville,{ 'attr': {'class': 'form-control'} }) }}
                                </div>
                                <div class="form-group">
                                    {{ form_row(form.message,{ 'attr': {'class': 'form-control'} }) }}
                                </div>

{#                                            <button type="submit" class="btn">Submit</button>#}
                                    <hr>
                                    <button type="submit" class="btn btn-success">Envoyer</button>

                            {{ form_end(form) }}
{#                            <a href="{{ path('app_contact') }}" type="submit" class="btn btn-theme3">Vendre</a>#}
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>


{% endblock %}
